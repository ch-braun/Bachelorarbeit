version: "3.9"
services:
  tensor:
    image: tensorflow/tensorflow
    volumes:
      - ./src:/home/code
      - ./data:/home/data
    command: bash -c "cd /home && python ./code/${PYTHON_ENTRYPOINT}"
